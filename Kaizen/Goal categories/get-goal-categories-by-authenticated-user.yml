info:
  name: get-goal-categories-by-authenticated-user
  type: http
  seq: 2

http:
  method: GET
  url: "{{api}}goalCategories/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
