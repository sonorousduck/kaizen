info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: "{{api}}public/users/login"
  body:
    type: json
    data: |-
      {
        "email": "test3@test.com",
        "password": "12345678"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
