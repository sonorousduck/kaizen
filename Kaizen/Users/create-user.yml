info:
  name: create-user
  type: http
  seq: 1

http:
  method: POST
  url: "{{localhost}}/public/users"
  body:
    type: json
    data: |-
      {
        "firstName": "test",
        "lastName": "anderson",
        "email": "test3@test.com",
        "password": "12345678"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
