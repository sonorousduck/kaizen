info:
  name: get-goal-by-id
  type: http
  seq: 2

http:
  method: GET
  url: "{{api}}goals/{{goalId}}"
  auth: inherit

runtime:
  variables:
    - name: goalId
      value: f6e6ab9c-eca4-4e1d-92e1-5a1ba90a6e9a

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
