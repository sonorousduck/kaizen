info:
  name: delete-by-id
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{api}}goals/{{goalId}}"
  auth: inherit

runtime:
  variables:
    - name: goalId
      value: 933a911d-aad7-47dc-a786-0ba454a3d13a

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
